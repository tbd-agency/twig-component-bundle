<span{{ attributes.defaults({'class': block('badge', tbd_twig_component_template)|trim ~ ' ' ~ this.extraClasses}) }}>
{%- if label is defined and label is not empty -%}
    {{ label|raw }}
{%- else -%}
    {% block content %}{% endblock content %}
{%- endif -%}
</span>
