<div{{ attributes.defaults({
    'class': block('card', tbd_twig_component_template)|trim ~ ' ' ~ this.paddingClasses ~ (shadow ? ' ' ~ block('card_shadow', tbd_twig_component_template)|trim : ''),
}) }}>
    {% block content %}{% endblock %}
</div>
