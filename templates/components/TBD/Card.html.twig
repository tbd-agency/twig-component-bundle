<div{{ attributes.defaults({
    'class': block('card_classes', tbd_twig_component_template)|trim ~ ' ' ~ this.paddingClasses ~ (shadow ? ' ' ~ block('card_shadow_classes', tbd_twig_component_template)|trim : ''),
}) }}>
    {% block content %}{% endblock %}
</div>
